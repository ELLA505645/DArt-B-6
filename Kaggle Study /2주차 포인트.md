데이터받을때 사용하기 좋은 이름

df_raw  # 원본
df_clean  # 정제
df_feat  # 파생변수 포함
df_model  # 모델 입력용

*파생변수: 새로운 변수를 만드는 것
정가-할인가 = 할인가격 생성 → difference_price

*제일 기본적인 전처리 방식
# Data Cleaning & Preparation

~~~
1. Changing Data Types of Columns from object to Floats
2. Filling in Missing Information
3. Checking For Duplicate Rows
4. Splitting Long Strings -> 'catagory' 'about_product' 등등
5. Creating Various New Columns
~~~

strip(): 공벡제거

display.max_columns : 모든 컬럼이 보이도록 설정

Encoding하는 이유: 모든 문자형을 숫자로 바꿔주는 과정 -> 컴퓨터가 인식해야하기 때문에
문자가 들어가면 오류가 뜨기 때문에 숫자로 바꿔줌
모델링할때 전처리로 인코딩을 해준다


*value_counts 사용법

~~~python
#1
df['rating'].value_counts()
#2
df.value_counts('rating')
~~~

1. 1번은 결측치 미포함 / 2번은 포함 
2. 1번은 단일 컬럼 인덱싱 / 2번은 multiindex가 가능함 ex) rating과 category 조합의 고유값이 얼마나 존재하는지 알고 싶은 경우 사용가능


query = DataFrame에서 조건식으로 행을 걸러내는 메서드


* t-test
purpose: 카테고리가 electronics인 상품들의 평균 평점, clothing인 상품들의 평균 평점이 통계적으로 차이가 있는지

H0: 평균 평점이 같다 

H1: 평균 평점이 다르다 (양측검정)

t-통계량은 두개의 평점을 비교하는 것.
평균의 차이가 분자로 들어감

평균 평점이 비슷할수록 통계량의 절대값이 작아질 것임
(p-value가 커짐)
-> 귀무가설을 기각할만한 근거가 불충분

<img width="1170" height="1018" alt="image" src="https://github.com/user-attachments/assets/42b80504-f405-48f0-82cc-847bfb1ba152" />

t는 두개의 값의 차이를 나타냄
t가 작아질수록 두개의 값의 차이가 적다는 거니까
귀무가설에 더욱 가까워지게 됨


❓ 데이터를 그냥 받는거랑 DataFrame으로 받는거랑 차이점이 뭘까?

- 그냥 받을때는, Series로 타입이 유지되는 경우
reviewer_id_exp = reviewer_id_split.explode()
*따로 감쌀 필요없이 그대로 출력해도 됨

- 데이터프레임으로 받을때는, Series를 DataFrame으로 받고 싶은 경우
df21 = pd.DataFrame(data=reviewer_id_clean)
*전체를 감싸서 바꾸고 싶을 경우 사용

두개를 잘 구분하는 것이 중요할 것 같다!
